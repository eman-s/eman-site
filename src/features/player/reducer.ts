const initialState = {
  position: [0, 0],
};

const playerReducer = (state = initialState, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default playerReducer;
